# 券商 FPGA 行情卡规格 v1

日期：2026-09-01  
板卡：**AMD Alveo UL3524**（`A-UL3524-P16G-PQ-G` / `XCVU2P-3FSVJ2104E`）  
范围：沪深 L2 解码 + A/B 择优 + 硬核组播 / 软核 DMA。**v1 不在 FPGA 内维护全深订单簿。**

---

## 0. 产品形态

同一 bitstream，两个交付面（招标要的硬核/软核）：

| 交付 | 出站 | 客户怎么用 |
|---|---|---|
| **硬核** | FPGA 口直接 UDP 组播（切穿、每客户过滤/限速） | 托管机房插光口 |
| **软核** | 同一归一化总线 → PCIe DMA → 主机再发 / AMA 类 API | 不能直连光口的 OMS、信创主机、调试 |

解码、择优、过滤只做一次。禁止硬核/软核两套解码。

v1 不做：片上全深建簿、客户策略、L1 矩阵、期货逻辑（口预留）。10 档快照字段原样转发，逐笔当事件流，不在 FPGA 里模拟撮合。

---

## 1. 器件与口表

UL3524：4×QSFP-DD = 32×10/25G，PCIe Gen4 x8，1PPS IN，72 MB QDR-II+（v1 热路径不用，留给 v2 簿），16 GB DDR4（日志/回放，不进切穿）。

全部 10GBASE-R。25G 电气预留，逻辑 v1 不启用。

### Q0 — 交易所入（8×10G）

| Lane | 方向 | 信号 | 备注 |
|---|---|---|---|
| Q0.0 | RX | 沪 L2 A | STEP/FAST 或 Binary，以托管合同为准 |
| Q0.1 | RX | 沪 L2 B | 与 A 同内容，择优 |
| Q0.2 | RX | 深 L2 A | Binary 组播（快照/指数/逐笔可同纤） |
| Q0.3 | RX | 深 L2 B | |
| Q0.4 | RX/TX | 沪 MDGW TCP | 主推或缺口回补，TOE |
| Q0.5 | RX/TX | 深 TCP 回补 | 组播缺口时用，不进切穿 |
| Q0.6 | RX | 预留 期货 A | v1 PHY 起来，逻辑 swallow |
| Q0.7 | RX | 预留 期货 B | 同上 |

### Q1 — 硬核客户 0–7（每 lane 一户，10G TX；RX 可关）

### Q2 — 硬核客户 8–15

### Q3 — 上联 / 运维

| Lane | 用途 |
|---|---|
| Q3.0–Q3.3 | 硬核溢出 16–19，或级联到第二张卡 / L1 |
| Q3.4 | 镜像 / 监管抽头（只读，延迟不保证） |
| Q3.5 | 带外管理（可选 1G 走板载，不占这口） |
| Q3.6–Q3.7 | 空，25G 试验 |

**v1 卖点密度：16 路硬核客户 / 卡。** 超过走软核或第二张卡，不做 L1。

时钟：入站 MAC/解码/择优/硬核 TX 同一 **322.265625 MHz** 域。PCIe 250 MHz，跨域只允许归一化事件 FIFO。1PPS 打 SOP 时间戳。

---

## 2. 模块（热路径）

```
Q0 MAC×N @322
    → udp_strip / tcp_pay_stub
    → dec_szse_bin  ‖  dec_sse_fast  ‖  dec_sse_bin   # A/B 各实例
    → arb_nway          # 按 seq+ch 择优，必须在解码之后
    → event_merge
    → sym_cam
    → event_bus
         ├─→ mcast_eng ×16   # 硬核，不准反压
         └─→ dma_pack        # 软核
```

择优要交易所序号，所以 **arb 在解码后**。详见 `docs/module-design-v1.md`。

### 2.1 买的 IP（第一年不自研）

- 10G 切穿 MAC/PCS（GTF RAW/MAC 参考或 Exegy/DesignGateway）
- 沪/深 TCP TOE（只给回补和沪 MDGW，不给客户出站）
- PCIe DMA / QDMA、板级 BSP

### 2.2 `arb_nway`

- 键是 **channel+seq**，不是时间戳
- 策略：先到且推进序号的胜；重复丢；缺口 **不堵** 切穿，只打 flag，回补走 Q0.4/Q0.5 慢路径
- 沪、深两套独立状态；资源：每通道 BRAM 滑窗

### 2.3 解码（三选二必做，期货不做）

| 模块 | 协议 | 切穿方式 | v1 必做 |
|---|---|---|---|
| `dec_szse_bin` | 深 Binary V5：快照、指数、逐笔委托、逐笔成交 | 定长字段，length+type 齐就开始抽 | **是** |
| `dec_sse_fast` | 沪 L2 STEP/FAST（10 档 + 50 档队列 + 逐笔） | 模板 FSM，模板主机加载 | **是**（沪增值 L2 仍常见） |
| `dec_sse_bin` | 沪 MDGW Binary（M101/M201…） | 定长，同深市风格 | **是**（与 FAST 做编译开关或双实例，口上只启用一种） |

每个解码器输出同一 `event_t`，禁止各写各的下游。

华锐实验室锚点（不是指标承诺）：深解码 ~130 ns，沪 ~210 ns。v1 内部预算：**深 SOP→event &lt; 300 ns，沪 FAST SOP→event &lt; 500 ns**（含 MAC，不含客户口排队）。对外 SLA 另写，不要印这些数到标书。

### 2.4 `event_t`（归一化总线，64B 对齐）

| 字段 | 宽 | 说明 |
|---|---|---|
| `ts_ns` | 64 | 1PPS + SOP 计数 |
| `seq` | 32 | 源通道序号 |
| `symbol_id` | 16 | 内部 ID，0=未命中 CAM（默认丢，可配转发） |
| `exch` | 2 | 0 沪 / 1 深 |
| `ch` | 4 | 快照 / 逐笔委托 / 逐笔成交 / 指数 / 队列 / 状态 |
| `msg_type` | 8 | 原类型 |
| `flags` | 8 | A/B 胜者、缺口、FCS 稍后失败 |
| `px` | 64 | 定点，单位 10⁻⁴ 元（A 股） |
| `qty` | 32 | |
| `side` | 2 | |
| `order_id` | 64 | 逐笔才有 |
| `level` / `queue_pos` | 8+8 | 快照档、50 档队列位 |
| `raw_ptr` | 16 | 可选：挂原始报文到 DMA 侧带 |

AXI-ST，`tlast` 按消息。II=1。快照 10 档打成 10 个 beat 或一条 packed beat（实现选后者，少 FIFO）。

### 2.5 `sym_cam`

- 容量 v1：**8192** 内部 ID（覆盖沪深股票+基金+债券常用集；全市场加哈希升级）
- 主机运行时写，双缓冲热更，切表不得抖 &gt; 1 拍
- 未命中默认丢（硬核客户禁刷全市场，除非买了全市场权限）

### 2.6 硬核 `mcast_eng`

每客户独立：

- 使能、UDP 组、VLAN、源/目的 MAC
- 代码位图或 ID 列表（v1：每户 **1024** ID，超出走软核）
- 通道掩码（只要逐笔 / 只要快照）
- 限速（token bucket，超限丢并计数，不反压热路径）
- 切穿：`event_t` → UDP 载荷，模板头预计算

16 个引擎并行。客户之间不共享 FIFO 深度以外的状态。

### 2.7 软核 `dma_pack`

- 大页、多通道（按客户或按通道），门铃
- 与硬核同一 `event_t`，可附 raw
- 主机软件：AMA 风格 API、组播转发、开盘与 FPGA 对账（软件金解码）

---

## 3. 资源与时钟（设计约束，非综合结果）

| 块 | 约束 |
|---|---|
| 热路径时钟 | 322.265625 MHz，单域 |
| LUT | VU2P 787K。v1 无全深簿，目标占用 &lt; 60%，留 择优+FAST |
| URAM | 代码表、每户过滤；不用来建 L3 |
| QDR | v1 不接热路径 |
| DDR4 | 环形日志、回放，PCIe 时钟域 |

若 FAST + 16 户过滤综合超 70%，砍硬核客户到 8，或换 **XUP-VV8 VU13P**（口同样 4×QSFP-DD，GTY 不是 GTF；行情卡解码是 100 ns 级，GTF 不是瓶颈）。

---

## 4. 运维 / 监管

- 每口：SOP 计数、择优胜负、缺口、FCS 晚失败、限速丢包
- 开盘前：软件金解码 vs FPGA `event_t` 对账，错则该通道不进硬核
- 双镜像 bitstream，看门狗
- 硬核客户权限、代码表、限速全部审计日志（主机侧）

---

## 5. 明确不做（v1）

- FPGA 全深 / 逐笔重建簿（国内公开产品也多数未钉死在 FPGA；v2 再上，QDR 才进热路径）
- 报单、风控（另一张卡）
- 自研 L1
- 25G RS-FEC
- 客户自定义 RTL 槽

---

## 6. RTL 交付切分（给实现）

1. `mac_wrap` 买 IP  
2. `arb_nway`  
3. `dec_szse_bin`  
4. `dec_sse_fast` + 模板加载  
5. `dec_sse_bin`  
6. `sym_cam`  
7. `event_bus`  
8. `mcast_eng`  
9. `dma_pack`  
10. `telem`

仿真：深 Binary 逐笔突发 + 沪 FAST 快照；A/B 乱序；故意缺口。对账模型用软件解码器，不拿 STAC-T0。
